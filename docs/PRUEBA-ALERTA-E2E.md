# Prueba de alerta extremo a extremo — BORRAR

Este fichero **no es contenido**. Existe solo para que un push a una rama
`claude/weekly-post/**` dispare `.github/workflows/weekly-post-pr.yml` y el
fundador compruebe que le llega el aviso de GitHub a su correo.

No hay artículo detrás. No toca `app/blog/**` ni `lib/blog/posts.ts`, así que
no puede renderizarse como un post ni aparecer en el índice del blog.

Su PR se cierra en cuanto el fundador confirme que recibió el email.
